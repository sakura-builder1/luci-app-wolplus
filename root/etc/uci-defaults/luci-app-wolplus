#!/bin/sh

# 修复 rpcd ucode 脚本执行权限（rpcd 仅加载带可执行位的脚本）
chmod 0755 /usr/share/rpcd/ucode/luci.wolplus 2>/dev/null

# 清理缓存并重载 rpcd
rm -f /tmp/luci-indexcache.*
rm -rf /tmp/luci-modulecache/
/etc/init.d/rpcd reload 2>/dev/null

exit 0
